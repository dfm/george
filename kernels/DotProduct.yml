name: DotProductKernel
stationary: false

doc: |
    The dot product kernel

    .. math::

        k(\mathbf{x}_i,\,\mathbf{x}_j) = \mathbf{x}_i \cdot \mathbf{x}_j

    with no parameters.

value: return x1 * x2;

grad:
    x1: return x2;
    x2: return x1;
